\documentclass[letterpaper,10pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=0.5in]{geometry}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{hyperref}
\usepackage{xcolor}

\definecolor{accent}{HTML}{2563EB}

\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlist[itemize]{nosep, leftmargin=1.5em, topsep=2pt}
\titleformat{\section}{\large\bfseries\color{accent}}{}{0em}{}[\color{accent}\titlerule]
\titlespacing*{\section}{0pt}{8pt}{4pt}

\begin{document}

%# --- Header ---
\begin{center}
  {\LARGE\bfseries\color{accent} \VAR{header_data.name | default('Your Name', true) | escape_latex}} \\[6pt]
  \small
  \BLOCK{if header_data.email}\href{mailto:\VAR{header_data.email | escape_latex}}{\VAR{header_data.email | escape_latex}}\BLOCK{endif}
  \BLOCK{if header_data.phone} $\cdot$ \VAR{header_data.phone | escape_latex}\BLOCK{endif}
  \BLOCK{if header_data.linkedin} $\cdot$ \href{https://\VAR{header_data.linkedin | escape_latex}}{\VAR{header_data.linkedin | escape_latex}}\BLOCK{endif}
  \BLOCK{if header_data.github} $\cdot$ \href{https://\VAR{header_data.github | escape_latex}}{\VAR{header_data.github | escape_latex}}\BLOCK{endif}
  \BLOCK{if header_data.website} $\cdot$ \href{https://\VAR{header_data.website | escape_latex}}{\VAR{header_data.website | escape_latex}}\BLOCK{endif}
\end{center}

\vspace{2pt}

%%j2 for section in sections
%%j2 if section.entries

\BLOCK{if section.type == 'education'}
\section{\VAR{section.title | escape_latex}}
%%j2 for item in section.entries
\textbf{\VAR{item.institution | default('') | escape_latex}} \hfill \VAR{item.location | default('') | escape_latex} \\
\textit{\VAR{item.degree | default('') | escape_latex}} \hfill \textit{\VAR{item.end | default('') | escape_latex}}
%%j2 if item.coursework
\begin{itemize}
  \item \VAR{item.coursework | escape_latex}
\end{itemize}
%%j2 endif
\vspace{2pt}
%%j2 endfor
\BLOCK{endif}

\BLOCK{if section.type == 'experience'}
\section{\VAR{section.title | escape_latex}}
%%j2 for item in section.entries
\textbf{\VAR{item.role | default('') | escape_latex}} \hfill \VAR{item.location | default('') | escape_latex} \\
\textit{\VAR{item.company | default('') | escape_latex}} \hfill \textit{\VAR{item.start | default('') | escape_latex} -- \VAR{item.end | default('') | escape_latex}}
%%j2 if item.bullets
\begin{itemize}
%%j2 for bullet in item.bullets
  \item \VAR{bullet.text | escape_latex}
%%j2 endfor
\end{itemize}
%%j2 endif
\vspace{2pt}
%%j2 endfor
\BLOCK{endif}

\BLOCK{if section.type == 'project'}
\section{\VAR{section.title | escape_latex}}
%%j2 for item in section.entries
\textbf{\VAR{item.name | default('') | escape_latex}}
\BLOCK{if item.tech_stack} $\cdot$ \textit{\VAR{item.tech_stack | escape_latex}}\BLOCK{endif}
\BLOCK{if item.url} $\cdot$ \href{https://\VAR{item.url | escape_latex}}{\small \VAR{item.url | escape_latex}}\BLOCK{endif}
%%j2 if item.bullets
\begin{itemize}
%%j2 for bullet in item.bullets
  \item \VAR{bullet.text | escape_latex}
%%j2 endfor
\end{itemize}
%%j2 endif
\vspace{2pt}
%%j2 endfor
\BLOCK{endif}

\BLOCK{if section.type == 'skills'}
\section{\VAR{section.title | escape_latex}}
\begin{itemize}[label={}]
%%j2 for item in section.entries
  \item \textbf{\VAR{item.name | default('') | escape_latex}:} \VAR{item['items'] | default([]) | join(', ') | escape_latex}
%%j2 endfor
\end{itemize}
\BLOCK{endif}

\BLOCK{if section.type == 'generic'}
\section{\VAR{section.title | escape_latex}}
\begin{itemize}
%%j2 for item in section.entries
%%j2 if item.text | default('')
  \item \VAR{item.text | escape_latex}
%%j2 endif
%%j2 endfor
\end{itemize}
\BLOCK{endif}

%%j2 endif
%%j2 endfor

\end{document}
